export interface ThemedProps {
  background: string;
  card: string;
  text: string;
  border: string;
  notification: string;
  primary: string;
  button: {
    primary: {
      backgroundColor: string;
    };
  };
}
